package com.bercik9991.domain

import spock.lang.Specification

/**
 * @CsvEditorSpec specification defines conditions that
 * the CSV Editor should meet to ensure
 * correct data import, addition, edition, and export.
 */
class CsvEditorSpec extends Specification {
    def 'should pass dummy CSV Editor spec'() {
        expect:
            true
    }
}
